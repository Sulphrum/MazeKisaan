# Create a private API key in your data.gov.in account, then set it only in
# your terminal or deployment secret manager. Never commit the real value.
DATA_GOV_IN_API_KEY=replace_with_your_personal_key

